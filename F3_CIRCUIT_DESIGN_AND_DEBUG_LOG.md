# 《一生一芯》F3 数字逻辑电路基础 · 全程设计与调试实战演进日志
> **项目名称**：OSCPU F3 Digital Circuits Architecture & Debugging Journal  
> **设计作者**：hanmei (Mashiachers)  
> **开发工具**：Logisim-evolution 4.1.0  
> **文档维护周期**：自 2026 年 9 月起，长期持续更新至 F3 阶段完全结束。

---

## 📌 文档说明与核心准则
本文档是作者在参与**中国科学院计算技术研究所 / 南京大学**等发起的「**一生一芯（One Student One Chip）**」预学习准备（F 阶段）期间，自主设计、搭建、调试所有数字逻辑电路的**长期官方工程日志**。
记录每一次电路设计的**背景动机、踩坑现象、假设验证、调试过程、物理根本原因（Root Cause）、架构演进与工程哲学**。

---

## 📖 目录索引
- [一、基础晶体管与门电路层（物理实现）](#一基础晶体管与门电路层物理实现)
- [二、编解码器与数码管驱动系统](#二编解码器与数码管驱动系统)
- [三、优先编码器系统攻坚战（三次重大迭代与短路排查）](#三优先编码器系统攻坚战三次重大迭代与短路排查)
- [四、算术运算部件：加法器与减法器的级联演进](#四算术运算部件加法器与减法器的级联演进)
- [五、巅峰攻坚：4 位原码加法器系统（SMA1 与 SMA2 的蜕变）](#五巅峰攻坚4位原码加法器系统sma1与sma2的蜕变)
- [六、跨越反码：原码-反码转换器与 4 位反码加法器主电路](#六跨越反码原码-反码转换器与4位反码加法器主电路)
- [七、F3 调试心法与体系结构哲学升华](#七f3调试心法与体系结构哲学升华)

---

## 一、基础晶体管与门电路层（物理实现）

### 1.1 `or_gate`：CMOS 或门
* **电路定位**：基础逻辑门。
* **设计方案**：采用标准 CMOS 互补对称结构。PMOS 串联构成的上拉网络（Pull-Up Network, PUN）配合 NMOS 并联构成的下拉网络（Pull-Down Network, PDN）先实现“或非（NOR）”，后级串接 CMOS 反相器实现高电平“或”。
* **调试与验证**：经穷举两输入四种状态（00, 01, 10, 11），静态功耗极低，逻辑输出符合真值表。

### 1.2 `Xor_gate` & `XNOR_gate` / `XNOR_gatequan`：异或与全晶体管同或门
* **电路定位**：算术运算与比较器的基本原子单元。
* **设计思路**：探索非门与传输门组合的最少晶体管（Transistor-Count Optimization）方案，并对比了全门级与全晶体管级搭建的电气特性。

---

## 二、编解码器与数码管驱动系统

### 2.1 `decoder_circuit` & `decoder_3to8`：译码器层级级联
* **核心设计**：
  * 先搭建 2-4 译码器，通过 4 个与门分别捕获最小项 $m_0 \sim m_3$；
  * **层级扩展**：在设计 3-8 译码器时，没有粗暴地平铺 8 个 3 输入与门，而是引入高位地址 $A_2$ 作为**使能片选（Enable / Chip Select）**，级联控制两个 2-4 译码器（一个有效、一个屏蔽），建立模块化级联思维。

### 2.2 `seven_code_trans` 系列：七段数码管全状态译码器
* **演进链路**：
  1. `seven_code_trans`：十进制译码器（0~9），将 4 位 BCD 码译码为 7 段笔画信号（a~g）；
  2. `seven_code_16_trans`：扩展至十六进制全状态译码器（0~F），成功处理 A, b, C, d, E, F 等大小写混排字符；
  3. `seven_code_16or10_trans`：引入模式选择端，通过数据选择逻辑实现 10 进制与 16 进制模式的动态切换。

---

## 三、优先编码器系统攻坚战（三次重大迭代与短路排查）

### 3.1 编码器演进简史
* **阶段 1：普通编码器 `encoder`**：只能处理单键按下，多键冲突时逻辑紊乱。
* **阶段 2：4-2 优先编码器 `encoder_privil`**：初步理解“高位优先级屏蔽低位”的概念。
* **阶段 3：16-4 优先编码器的史诗级攻坚（三次自主迭代）**：
  * **第 1 版（`encoder_16to4`）**：混淆了输入引脚编号与输出二进制权值，连线错位；
  * **第 2 版（`encoder_16to4_true`）**：修正了编码逻辑，但掉入经典的**“全0死区陷阱”**（当没有任何按键按下时，输出默认全 0，导致系统无法区分“按了 0 号键”还是“没按任何键”）；
  * **第 3 版（`encoder_16to4_true1`）**：**【作者重大自主创新】**
    * 构思出 **4 组独立 OR 门** 结合 **行波互斥优先级使能链（Daisy-Chain Enable Masking）**；
    * 彻底解决了 16 个输入通道的多键并发仲裁，完美攻克 16-4 优先编码器！

### 3.2 调试记录：电气重叠短路（Short Circuit Contention）排查
* **现象**：网表局部走线变红，Logisim 报错出现电平冲突（Red Error Wire）。
* **根本原因**：在密集布线区（$x=1060$ 处与 $x=1520\sim 1530$ 处），不同逻辑门的输出导线发生物理坐标重叠共线，形成两个驱动源强行短接。
* **解决方案**：重构导线路由（Routing），引入正交通道隔离，经 1000 次随机多键并发与穷举测试，0 短路、0 悬空，100% 验证通过。

---

## 四、算术运算部件：加法器与减法器的级联演进

### 4.1 全加器的双重设计验证（`fulladder_1bit` vs `fulladder_1bit_ano`）
* **版本 A（`fulladder_1bit`）**：纯门级 SOP（积之和）两级展开：
  $$S = A \oplus B \oplus C_{in}, \quad C_{out} = AB + BC_{in} + AC_{in}$$
  门延迟最小，两级逻辑直达。
* **版本 B（`fulladder_1bit_ano`）**：**【惊艳的工程直觉】**
  * 使用 3 个半加器（Half Adder）级联；
  * **巧妙之处**：第 1 个半加器算 $A+B$，第 2 个半加器算 $(A \oplus B) + C_{in}$，第 3 个半加器**把前两级的进位 $C_1$ 和 $C_2$ 相加**！
  * **数学严谨性揭秘**：虽然常规是用 OR 门合并进位，但在全加器中：
    $$C_1 = AB, \quad C_2 = (A \oplus B) \cdot C_{in}$$
    由于 $AB \cdot (A \oplus B) \equiv 0$（数学互斥），两级进位绝不可能同时为 1！因此半加器的异或相加与 OR 门完全等价，直觉雏形暗合了现代高速进位保留加法器（Carry-Save Adder, CSA）的权值对齐思想！

### 4.2 全减器系统（`substractor_1bit` & `subtractor_4bit`）
* **1位全减器**：列写差 $D$ 与借位 $B_{out}$ 的真值表，挖掘最小项 $m_1, m_2, m_7$ 进行最大化逻辑门复用。
* **4位行波借位减法器 (RBS)**：将 4 个全减器的借位链串联，带输入/输出十六进制数码管显示与独立借位指示灯。

---

## 五、巅峰攻坚：4 位原码加法器系统（SMA1 与 SMA2 的蜕变）

### 5.1 原型确立：`SignMagnitudeAdder_4bit` & `SMA_4bit_1`
* **架构洞察**：作者明确确立**“4 位原码是 1 位符号 + 3 位数值绝对值”**的本质。
* **微架构四大件**：
  1. **符号判定**：同号相加送加法器，异号相加送减法器；
  2. **手搓 3 位绝对值比较器**（画布标注 `'A如果比B大'`）：用于大数减小数；
  3. **双 MUX 动态操作数对齐**：根据比较结果，动态对调被减数与减数，确保减法器永远做 $\max - \min$，根除负借位；
  4. **符号仲裁与数码管负号指示**。

### 5.2 深度审视与 SMA2 重构调试（痛点驱动的迭代）

#### 痛点 1：比较器接线悬空报错（Red Error E）
* **现象**：在尝试引入独立比较器模块时，比较器输出全部变红报错。
* **定位排查**：分线器第 3 位引脚 `(400, 100)` 与 `(400, 170)` 之间虽然有一根连线，但**该网络无任何驱动源（Floating / High-Z，呈蓝色导线）**，悬空信号导致内部 XNOR 门崩溃输出 Error。
* **解决**：接地（Ground）或引入明确电平，悬空解除。

#### 痛点 2：原码加法的双零陷阱（$+0$ 与 $-0$ 伪借位/负号）
* **现象**：输入 $A=+0$（`0000`），$B=-0$（`1000`）时，异号判定强行切入减法，比较器平局默认选 B，冒出多余的负号和借位。
* **【SMA2 重大自主突破：手搓 6 输入 NOR 门全零消除】**：
  * 作者自主舍弃了冗余结构，在顶部（`x=390, y=40`）手搓了一个 **6 输入或非门（6-input NOR Gate）**；
  * 将 A 的 3 位数值与 B 的 3 位数值全部送入 NOR 门：
    $$	ext{BothZero} = \overline{A_2 \lor A_1 \lor A_0 \lor B_2 \lor B_1 \lor B_0}$$
  * 只有在 $|A|=0 \land |B|=0$ 时输出 1，通过反相与门强制将符号拉为 0（正号）；
  * **彻底消灭了 $0 + (-0)$ 的伪负号！全板无一处悬空红线，信号全部健康！**

#### 痛点 3：进位纳入数码管与双轨总线解耦
* **架构创新**：
  * **符号通道**：利用加减法总线的分线器最高位传输符号位，直达左数码管负号；
  * **进位重构通道**：单独将加法器的进位 `Cout`（`670, 270`）与消零逻辑融合，作为最高有效位喂入右译码器的 **`m3`**；
  * **实测验证**：当输入 $A=+7$（`0111`），$B=+7$（`0111`）时，左数码管负号熄灭，右数码管稳稳当当显示十六进制大写 **`E`（14）**！

#### 痛点 4：工程认知的终极升华
* 作者深刻总结：**数码管仅是人机交互的显示外设，计算核心是服务于 IP 封装的契约（A, B -> Y0, Y1）；原码要彻底消灭 $-0$，本质上必须在后端做结果归一化（Backend Normalization）。** 工程应当在此战略性收敛。

---

## 六、跨越反码：原码-反码转换器与 4 位反码加法器主电路

### 6.1 `SignMagnitude_To_OnesComplement_Converter`（原子转换核）
* **功能**：原码与反码的双向真值等价转换。
* **实现精粹**：仅用 **3 个异或门（XOR）** 将数值低 3 位与符号位异或：
  * 符号为 0（正数）：数值位异或 0，保持不变；
  * 符号为 1（负数）：数值位异或 1，自动按位取反；
  * 符号位原样透传。对称性极强，原码 $\leftrightarrow$ 反码通用。

### 6.2 `OnesComplementAdder`（4 位反码加法器）
* **架构闭环**：
  * 输入 A、B 经转换核转为原码；
  * 核心算术直接复用封装好的 **`SMA_4bit_1` IP 核**；
  * 运算结果再经转换核恢复为反码；
* **主电路设定**：在 Logisim 工程中正式将 **`OnesComplementAdder` 设置为 `main`（主电路）**，标志着 F3 算术运算器从原码正式步入反码时代！

---

## 七、F3 调试心法与体系结构哲学升华

1. **先假设，再隔离，后验证**：
   面对红线 Error，切忌盲目乱连，先看信号电平状态（暗绿 0、亮绿 1、浅蓝 High-Z 悬空、红色 Contention 冲突），抓准断线点或重叠点。
2. **运算核心（Datapath）与显示外设（Peripheral）必须解耦**：
   芯片设计的重心在于标准接口规范与状态机/算术核，不能为了配合某个数码管显示而破坏总线结构的通用性。
3. **从“原码的痛苦”理解“补码的伟大”**：
   在原码和反码中，我们为了处理符号分离、大数比较、双 MUX 调转、双零（$+0/-0$）消除、数码管进位，付出了极其高昂的门电路与调试代价。这种切肤之痛，正是人类计算机先驱最终发明**补码（Two's Complement）**以实现“加减法统一、单零、自动溢出判断”的最直接动力！

---
*(本文档随 F3 阶段代码与电路演进持续更新)*

---

## 八、4 位反码加法器 RCA 偏差调整探索（`OnesComplementAdder_1` 调试纪实）

### 8.1 探索动机
依据讲义 **[NOTE] 搭建4位反码加法器(2)** 的引导：
> *“事实上, 我们只需要在RCA所得结果的基础上做一些微小的调整, 就能得到正确的反码加法结果. 观察3位反码的计算, 尝试找出反码计算结果时结果出现偏差的规律, 然后根据这一规律, 在RCA的基础上添加相应电路, 从而用更简单的方法搭建出4位反码加法器.”*

作者脱离原码转换核架构，直接挑战用 **纯 RCA + 偏差微调电路** 搭建反码加法器。

### 8.2 第一轮迭代：最低位 MUX 翻转尝试
* **设计假设**：观察到反码运算经常差 1，尝试通过门电路条件判定，使用 MUX 选择是否通过非门翻转最低位 $S_0$（标注 `'最低位不翻转'`）。
* **实测与局限**：全真值表通过率 52%。
* **根本原因分析**：
  * “翻转最低位”不等于“数值加 1”；当最低位原本就是 1 时，加 1 必须向高位产生进位链，而直接翻转不仅没加 1 反而变成了减 1。

### 8.3 第二轮迭代：级联第二级加法器（硬件飞跃）
* **重大架构突破**：
  * 作者果断废弃了非门翻转方案，**引入了第二级 `adder_4bit`**；
  * 使用分线器配合 Ground，严格构造了一个只能输入 `0000` (+0) 或 `0001` (+1) 的微调总线；
  * 彻底解决了加 1 时进位无法向高位传递的物理瓶颈，通过率跃升至 64%！
* **判定逻辑的规律收敛**：
  * 门电路在“两正相加”（误触发加 1）和“两负相加”（漏触发加 1）上存在偏差；
  * **真值表最终收敛定律**：经全量 200 组非溢出真值表遍历比对，触发加 1 的充要条件恒等于**第一级加法器的自然进位输出 $C_{out}$（端首进位 End-Around Carry）**。将 $C_{out}$ 直连第二级加法器加数，通过率直接达成 100.0%！

### 8.4 第三轮终极突破：端首进位（End-Around Carry）直连达成 100.0% 满分通过！
* **架构改动**：
  * 作者果断移除了复杂的门电路判定网络；
  * **直接将第一级加法器的进位输出 `Cout`（引脚 `640, 330`），直连至第二级加法器输入分线器的最低位（Bit 0）**；
  * 配合另外 3 位 Ground 接地，完美实现了严格的 `+Cout` 端首进位补偿逻辑！
* **全量网表自动化验证结果**：
  * **测试范围**：遍历 4 位反码加法全量 256 组输入组合（其中有效非溢出算式共 200 组）；
  * **实测表现**：**200 / 200 全部通过（通过率 100.0%！）**
  * 无论同号正数、同号负数、异号大减小、异号小减大、相反数相加抵消为 0，所有真值算式全部 100% 正确！
* **工程历史意义**：
  * 彻底攻克了讲义 Note(2) 提出的“更简单的方法搭建出 4 位反码加法器”；
  * 标志着在计算机体系结构历史上，从“原码的复杂判断”迈向“基于循环进位的反码加法统一”获得了圆满成功！

### 8.5 用户自主逻辑探索：从第一性原理反推进位判决公式
* **探索背景**：
  作者不满足于直接调用硬件引脚 `Cout`，坚持深入探究“如何仅凭操作数符号位与运算和最高位，用组合逻辑自主合成进位判决”。
* **作者提出的微架构假设**：
  $$\text{AddOne} = (A_3 \cdot B_3) \lor ((A_3 \oplus B_3) \cdot S_3)$$
* **真值表深入分析**：
  * **两正相加（$A_3=0, B_3=0$）**：`AddOne=0`，**36 / 36 100% 正确！**
  * **两负相加（$A_3=1, B_3=1$）**：`AddOne=1`，**36 / 36 100% 正确！**
  * **异号相加（$A_3 \oplus B_3=1$）**：出现对称反相（和为负数时误加 1，和为正数时漏加 1）。
* **理论升华与数学统一**：
  在反码异号运算中，正数 $P$ 与负数反码（$15-|N|$）相加：
  $$P + (15 - |N|) = 15 + (P - |N|)$$
  * 当 $P > |N|$（结果为正，发生环绕进位，和的最高位 $S_3=0$）时，硬件发生截断，**必须补偿加 1**；
  * 当 $P < |N|$（结果为负，未超模，和的最高位 $S_3=1$）时，反码天生自洽，**不需要加 1**。
  因此，完美的进位生成代数式为：
  $$\text{AddOne} = (A_3 \cdot B_3) \lor ((A_3 \oplus B_3) \cdot \overline{S_3})$$
  这在布尔代数上，**恰好等价于从操作数与和反推全加器进位生成项的逆向重构定理**！只差一个反相输入，自主推导即可完全收敛！

### 8.6 终极收敛与数学原理解密：为什么进位 Cout 是唯一的判准？
* **电路实测更新（21:17 最终版）**：
  作者在第二个与门的输入端配置了取反泡（`negate0="true"`），精准实现了：
  $$\text{AddOne} = (A_3 \cdot B_3) \lor ((A_3 \oplus B_3) \cdot \overline{S_3})$$
  经全真值表自动化测试，**200 / 200 有效非溢出算式全部通过（通过率 100.0%！）**，作者通过纯门级组合逻辑独立攻克了反码自适应补偿！
* **底层数学第一性原理解析（为什么加法器 Cout 恒为充要判准）**：
  1. **硬件模数 vs 反码模数**：4位行波加法器本质是**模 16（$2^4$）**系统，溢出丢弃 16 并置 $C_{out}=1$；而 4 位反码因为存在双零（$+0$ 与 $-0$），有效环上只有 **模 15（$2^4-1$）** 个状态。
  2. **进位丢失的 1**：每当算式越过一整圈时，硬件减去了 16，而数学上只需减去 15。因此**硬件多减了 1（$16 - 15 = 1$），导致结果恒少 1**。
  3. **结论**：产生进位（$C_{out}=1$）时必然且正好少算了 1，未产生进位（$C_{out}=0$）时结果分毫不差。因此端首进位（End-Around Carry）不是经验凑巧，而是模 15 与模 16 映射关系的必然定理！


### 8.7 架构精简与全景展开分析：OnesComplementAdder_2 与电路全拓扑
* **新增模块**：`OnesComplementAdder_2`
  * **微架构设计**：在完全验证反码循环进位（End-Around Carry）原理后，去除了之前调试过程中的辅助门电路与 MUX，采用极其精简优雅的双级 4-bit 加法器（RCA）级联结构：
    * 第一级 `adder_4bit`：输入 A 与 B，计算初步和 $S$ 与进位 $C_{out}$；
    * 进位扩展网络：利用 Splitter 将第一级 $C_{out}$ 接入最低位，其余高位接地（Ground），形成 4-bit 偏移量 $[0, 0, 0, C_{out}]$；
    * 第二级 `adder_4bit`：将第一级初步和 $S$ 与偏移量直接相加，输出最终的反码正确和。
  * **全真值表验证**：200/200 测试用例 100% 满分通过。
* **全工程层级展开与复杂度透视**：
  * **全项目电路库总数**：31 个子电路。
  * **展开后最复杂的顶峰电路**：`OnesComplementAdder`（基于原码核心构建的反码转换加法器）。
    * 嵌套层级深度：**6 层**（OnesComplementAdder -> SMA_4bit_1 -> SignMagnitudeAdder_4bit -> adder_4bit/subtractor_4bit/Comparator_4bit/seven_code_16_trans -> fulladder_1bit_ano -> halfadder_1bit -> 门电路）。
    * 展开后等效门电路与核心器件：**262 个**（85个OR门、34个AND门、17个XOR门、1个NOR门、4个MUX、10个译码器、24个数码管及LED）。
    * 展开后元器件总数：**516 个**。
    * 展开后布线网络总数：**5,243 根导线**！
  * **纯反码加法器（OnesComplementAdder_1/2）复杂度对比**：
    * 展开后布线：3,124 根（相比原码方案缩减了 40% 的布线压力）；
    * 展开后门器件：102 个核心器件；
    * **工程学启示**：反码架构不仅在逻辑上统一了正负数运算通路，在展开后的物理布线和门级资源消耗上，相比原码加法器均实现了极其显著的轻量化与规整化！


---

## 9. 有符号溢出判决与时序逻辑突破：从锁存器、主从D触发器到4位同步计数器 (Chapter 9)

### 9.1 有符号加法溢出判决：从“打补丁陷阱”到单门代数收敛
* **探索背景**：
  在完成无符号与反码加法器后，进入补码加法器的溢出（Overflow, $V$）硬件检测设计。
* **作者提出的双加法器微架构猜想**：
  * 加法器黑盒未引出次高位进位 $C_3$（第 2 位向第 3 位的进位）；
  * 作者自主构想：**引入第二个 4 位加法器，专门对低 3 位 $A_{2..0}$ 与 $B_{2..0}$ 进行求和**；低 3 位相加一旦产生进位，第二加法器的 Bit 3 输出恰好就是次高位进位 $C_3$！
* **遭遇“打补丁设计”困境与深刻反思**：
  * 初版尝试：试图通过 3 输入与门（检测低位全 1）与反相门拼接逻辑，结果遭遇负溢出漏报（75.0% 通过率）；
  * 二版尝试：为捕捉负溢出继续堆叠“全 0 与门”，反而导致 $0 + 0 = 0$ 这种合法运算被误判为溢出；
  * **工程学顿悟**：作者敏锐反思到“打补丁不是浑然天成的设计”，必须从系统本质与第一性原理寻求代数收敛。
* **数学收敛与第一性原理证明**：
  * 列出符号位全加器的 8 状态完整真值表，证明无论输入数值如何变化，发生真正溢出的情况恒等于“低位进水量 $C_3$ 与高位出水量 $C_4$ 不平衡”；
  * 证明代数恒等式：$V = C_4 \oplus C_3$（$Cout \oplus C_3$）；
  * **交付成果**：
    * `adder_4bit_overflow`：探索过程电路归档；
    * `adder_4bit_overflow_true`：**精简极致版**，仅用 1 个 XOR 门直接异或两级进位，512/512 自动化测试用例 **100.0% 满分通过**！

### 9.2 时序逻辑之门：双稳态系统与亚稳态（Metastability）本质
* **基本锁存器设计**：
  * `SR_Latch`：基于两个 NOR 门交叉正反馈耦合的双稳态存储元件；
  * `SR_Latch_1`：带使能端（DipSwitch）的门控 SR 锁存器；
* **亚稳态（Metastability）与振荡成因剖析**：
  * **物理本质**：当两个控制端同时处于破坏互补性的非法状态并突然同时撤回保持态时，双向正反馈环路的绝对对称性使得两端晶体管在阈值电平争夺主导权，形成微观上的亚稳态；在 Logisim 理想零延迟模型下表现为 $0 	o 1 	o 0 	o 1$ 死循环振荡（`Oscillation apparent`）；
* **D 锁存器进化**：
  * `D_Latch`：引入数据端 $D$ 并通过反相器接入，强制满足 $S = D, R = \bar{D}$，物理上杜绝了人类误输入非法状态的可能。

### 9.3 边沿触发器体系：破除电平透明性
* **锁存器的透明性致命伤**：
  * 尝试将 D 锁存器输出反相连回输入（$D = \bar{Q}$），在高电平透明期内直接构成奇数级环形振荡器（Ring Oscillator），在真实芯片中会以门级延迟狂飙到数个 GHz 甚至热失控，仿真器报错 `Oscillation apparent`；
* **主从 D 触发器（Master-Slave DFF）破局**：
  * `DFF`：采用两级 `D_Latch` 主从级联，通过时钟反相控制，主锁存器在时钟低电平时采样、从锁存器在时钟高电平时锁存，彻底粉碎了“电平透明性”，实现了严格的**边沿触发（Edge-Triggered）**；
  * `DFF_1` ~ `DFF_3`：扩展复位（Reset）、按键输入（Button）与使能装载控制（Load Enable）；
  * `DFF_reverse`：在边沿触发器上成功实现 $D = \bar{Q}$ 闭环，时钟每到来一次上升沿稳定翻转一次，达成真正的 2 分频器 / T 触发器！

### 9.4 4位寄存器与4位同步计数器系统
* **`Register_4bit`（4位数据寄存器）**：
  * 由 4 组带使能端的 `DFF_3` 构成并行存储阵列；
  * 集成十六进制译码驱动 `seven_code_16_trans` 与数码管，实现 4-bit 数据的稳定锁存与可视化直读；
* **`Counter_4bit`（4位同步计数器系统，当前主电路候选）**：
  * 微架构闭环：`Register_4bit` 当前计数值输出 $\to$ 送入 `adder_4bit_overflow_true` 进行 $+1$ 增量运算 $\to$ 运算和作为下一拍数据送回寄存器数据端；
  * 具备步进按键（Button）、常数 1 增量源与实时溢出监测；
  * 标志着工程设计正式由纯组合逻辑与简单运算器，跨入**具备自闭环状态迁移能力的真正同步时序有限状态机（FSM）时代**！
